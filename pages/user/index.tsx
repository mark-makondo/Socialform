import type { NextPage } from 'next';

// components
import Layout from '../../components/Layout';

const feed: NextPage = () => {
    return <Layout>user</Layout>;
};

export default feed;
